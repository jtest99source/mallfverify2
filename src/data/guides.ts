import type { Guide } from "@/types/guide";

// Guías antiguas ocultadas — reemplazadas por las nuevas en Supabase.
// El script scripts/seed-guides.mjs las marca como 'hidden' en la base de datos.
export const guides: Guide[] = [];
